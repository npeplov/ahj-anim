export default class Gui {
  constructor() {
    this.collapse = document.querySelector('.collapse');
    this.collapseButton = this.collapse.querySelector('button');
    this.collapseDiv = this.collapse.querySelector('div');
  }
}
